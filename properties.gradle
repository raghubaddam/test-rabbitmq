project.ext {
    offline = 'false'
    javaVersion = JavaVersion.VERSION_1_8

    cargoTomcatUrl = 'http://ks64.phx.gapinc.dev/gapSoftware/tomcat/source/apache-tomcat-6.0.35.tar.gz'

    //Artifactory
    artifactoryUrl = 'http://artifactory.gapinc.dev/artifactory/'
    filesystemRepo = 'file:///tmp/artifactory/dummy-repo'

    //Repositories
    libsRepo     = "${artifactoryUrl}maven-repos"
    unstableRepo = "${artifactoryUrl}platform-unstable-local"

    //Uploading URLs
    uploadUrl = unstableRepo
    if (project.hasProperty('uploadLocal')) uploadUrl = filesystemRepo

    springBootVersion = '1.4.+'

    junitDependency = 'junit:junit-dep:4.11'
    hamcrestDependency = 'org.hamcrest:hamcrest-all:1.3'
    mockitoDependency = 'org.mockito:mockito-core:1.10.19'
    mockitoDependencyFix = 'org.objenesis:objenesis:2.2'
}